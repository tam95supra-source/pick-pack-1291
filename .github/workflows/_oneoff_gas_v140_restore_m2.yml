name: Oneoff GAS v140 Restore M2 Discovery

on:
  pull_request:
    branches: [ main ]
    paths:
      - 'ops/gas-v140-m2-trigger.txt'
      - '.github/workflows/_oneoff_gas_v140_restore_m2.yml'
      - 'google-apps-script/SERVICE_MIGRATION_M2.gs'
      - 'tools/apply_m2_gas_restore_patch.py'

concurrency:
  group: gas-live-deploy
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  deploy:
    if: github.event.pull_request.head.repo.full_name == github.repository && github.event.pull_request.head.ref == 'fix/gas-v140-restore-m2-discovery-20260824'
    runs-on: ubuntu-24.04
    timeout-minutes: 18
    env:
      GOOGLE_OAUTH_CLIENT_ID: ${{ secrets.GOOGLE_OAUTH_CLIENT_ID }}
      GOOGLE_OAUTH_CLIENT_SECRET: ${{ secrets.GOOGLE_OAUTH_CLIENT_SECRET }}
      GOOGLE_OAUTH_REFRESH_TOKEN: ${{ secrets.GOOGLE_OAUTH_REFRESH_TOKEN }}
      GAS_SCRIPT_ID: ${{ secrets.GAS_SCRIPT_ID }}
      GAS_DEPLOYMENT_ID: ${{ secrets.GAS_DEPLOYMENT_ID }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Materialize current GAS transforms plus M2 restore
        shell: bash
        run: |
          set -euo pipefail
          python3 -m py_compile tools/apply_s12_gas_patch.py tools/apply_s12_gas_health_patch.py tools/apply_s13_shared_history_gas_patch.py tools/apply_s15_local_first_gas_patch.py tools/apply_s17_sqlite_recovery_gas_patch.py tools/apply_m2_gas_restore_patch.py
          if ! grep -q "v0.4.2 S12 CURRENT-DAY CACHE / REPORT OVERRIDES" google-apps-script/PICK_PACK_API.gs; then python3 tools/apply_s12_gas_patch.py; fi
          if ! grep -q "report_engine:'S12_CURRENT_DAY'" google-apps-script/PICK_PACK_API.gs; then python3 tools/apply_s12_gas_health_patch.py; fi
          if ! grep -q "history_engine:'S13_SHARED_SESSION'" google-apps-script/PICK_PACK_API.gs; then python3 tools/apply_s13_shared_history_gas_patch.py; fi
          if ! grep -q "v0.4.2 S15 LOCAL-FIRST 45D SYNC" google-apps-script/PICK_PACK_API.gs; then python3 tools/apply_s15_local_first_gas_patch.py; fi
          if ! grep -q "v0.4.2 S17 SQLITE CRASH RECOVERY" google-apps-script/PICK_PACK_API.gs; then python3 tools/apply_s17_sqlite_recovery_gas_patch.py; fi
          python3 tools/apply_m2_gas_restore_patch.py
          grep -q "action === 'service_discovery'" google-apps-script/PICK_PACK_API.gs
          grep -q "ppM2OperationalRoute_(auth, body, 'enter'" google-apps-script/PICK_PACK_API.gs
          grep -q "function ppM2Discovery_" google-apps-script/SERVICE_MIGRATION_M2.gs
          grep -q "PP_M2_SERVICE_URL" google-apps-script/SERVICE_MIGRATION_M2.gs
          grep -q "script.external_request" google-apps-script/appsscript.json
          cp google-apps-script/PICK_PACK_API.gs /tmp/PICK_PACK_API.js
          cp google-apps-script/SERVICE_MIGRATION_M2.gs /tmp/SERVICE_MIGRATION_M2.js
          node --check /tmp/PICK_PACK_API.js
          node --check /tmp/SERVICE_MIGRATION_M2.js
          echo 'GAS v140 source/M2 gates: PASS'

      - name: Exchange Google OAuth and normalize IDs
        id: auth
        shell: bash
        run: |
          set -euo pipefail
          SCRIPT_ID=$(printf '%s' "$GAS_SCRIPT_ID" | tr -d '\r\n\t ')
          RAW=$(printf '%s' "$GAS_DEPLOYMENT_ID" | tr -d '\r\n\t ')
          DEPLOYMENT_ID="$RAW"
          if [[ "$RAW" =~ /s/([^/]+)/ ]]; then DEPLOYMENT_ID="${BASH_REMATCH[1]}"; fi
          RESP=$(curl -fsS https://oauth2.googleapis.com/token -H 'Content-Type: application/x-www-form-urlencoded' --data-urlencode "client_id=$GOOGLE_OAUTH_CLIENT_ID" --data-urlencode "client_secret=$GOOGLE_OAUTH_CLIENT_SECRET" --data-urlencode "refresh_token=$GOOGLE_OAUTH_REFRESH_TOKEN" --data-urlencode 'grant_type=refresh_token')
          TOKEN=$(python3 -c 'import json,sys;print(json.load(sys.stdin).get("access_token",""))' <<<"$RESP")
          [[ -n "$SCRIPT_ID" && -n "$DEPLOYMENT_ID" && -n "$TOKEN" ]]
          echo "::add-mask::$SCRIPT_ID"; echo "::add-mask::$DEPLOYMENT_ID"; echo "::add-mask::$TOKEN"
          echo "script_id=$SCRIPT_ID" >> "$GITHUB_OUTPUT"
          echo "deployment_id=$DEPLOYMENT_ID" >> "$GITHUB_OUTPUT"
          echo "token=$TOKEN" >> "$GITHUB_OUTPUT"

      - name: Update Apps Script project and existing live deployment
        id: deploy
        shell: bash
        env:
          SCRIPT_ID: ${{ steps.auth.outputs.script_id }}
          DEPLOYMENT_ID: ${{ steps.auth.outputs.deployment_id }}
          ACCESS_TOKEN: ${{ steps.auth.outputs.token }}
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json
          from pathlib import Path
          b=Path('google-apps-script'); files=[]
          for p in sorted(b.glob('*.gs')): files.append({'name':p.stem,'type':'SERVER_JS','source':p.read_text()})
          files.append({'name':'appsscript','type':'JSON','source':(b/'appsscript.json').read_text()})
          Path('/tmp/content.json').write_text(json.dumps({'files':files},ensure_ascii=False))
          PY
          H=$(curl -sS -o /tmp/content.out -w '%{http_code}' -X PUT -H "Authorization: Bearer $ACCESS_TOKEN" -H 'Content-Type: application/json' --data-binary @/tmp/content.json "https://script.googleapis.com/v1/projects/$SCRIPT_ID/content")
          [[ "$H" == 200 ]] || { cat /tmp/content.out; exit 1; }
          H=$(curl -sS -o /tmp/version.json -w '%{http_code}' -X POST -H "Authorization: Bearer $ACCESS_TOKEN" -H 'Content-Type: application/json' -d '{"description":"Pick Pack 1291 v140 restore M2 discovery and authority routing"}' "https://script.googleapis.com/v1/projects/$SCRIPT_ID/versions")
          [[ "$H" == 200 ]] || { cat /tmp/version.json; exit 1; }
          V=$(python3 -c 'import json;print(json.load(open("/tmp/version.json"))["versionNumber"])')
          python3 - "$SCRIPT_ID" "$V" <<'PY'
          import json,sys
          open('/tmp/deploy.json','w').write(json.dumps({'deploymentConfig':{'scriptId':sys.argv[1],'versionNumber':int(sys.argv[2]),'manifestFileName':'appsscript','description':'Pick Pack 1291 live API v140 M2 restore'}}))
          PY
          H=$(curl -sS -o /tmp/deploy.out -w '%{http_code}' -X PUT -H "Authorization: Bearer $ACCESS_TOKEN" -H 'Content-Type: application/json' --data-binary @/tmp/deploy.json "https://script.googleapis.com/v1/projects/$SCRIPT_ID/deployments/$DEPLOYMENT_ID")
          [[ "$H" == 200 ]] || { cat /tmp/deploy.out; exit 1; }
          echo "version=$V" >> "$GITHUB_OUTPUT"
          echo "GAS_DEPLOYMENT_VERSION=$V"

      - name: Resolve live WEB_APP URL
        id: webapp
        shell: bash
        env:
          SCRIPT_ID: ${{ steps.auth.outputs.script_id }}
          DEPLOYMENT_ID: ${{ steps.auth.outputs.deployment_id }}
          ACCESS_TOKEN: ${{ steps.auth.outputs.token }}
        run: |
          set -euo pipefail
          curl -fsS -H "Authorization: Bearer $ACCESS_TOKEN" "https://script.googleapis.com/v1/projects/$SCRIPT_ID/deployments/$DEPLOYMENT_ID" > /tmp/deployment.json
          python3 - <<'PY' > /tmp/weburl
          import json
          j=json.load(open('/tmp/deployment.json')); eps=j.get('entryPoints',[])
          web=[e.get('webApp',{}) for e in eps if e.get('entryPointType')=='WEB_APP']
          assert web and web[0].get('url'), 'WEB_APP entry point missing'
          cfg=web[0].get('entryPointConfig',{})
          assert cfg.get('access')=='ANYONE_ANONYMOUS',cfg
          assert cfg.get('executeAs')=='USER_DEPLOYING',cfg
          print(web[0]['url'])
          PY
          URL=$(cat /tmp/weburl); echo "url=$URL" >> "$GITHUB_OUTPUT"

      - name: Verify live discovery, health and OTA channel isolation
        shell: bash
        env:
          WEB_URL: ${{ steps.webapp.outputs.url }}
        run: |
          set -euo pipefail
          OK=false
          for i in $(seq 1 18); do
            curl -fsSL -H 'content-type: application/json' "$WEB_URL" -d '{"action":"service_discovery","_app_channel":"BETA","_app_version":"0.4.2-beta.63","_device_id":"ci-v140-probe"}' > /tmp/discovery.json || true
            if python3 - <<'PY'
          import json
          try:
            j=json.load(open('/tmp/discovery.json')); a=j.get('authority') or {}
            ok=(j.get('ok') is True and j.get('authority_mode')=='SERVICE_PRIMARY' and j.get('service_url')=='https://pickpack.1291.workers.dev' and a.get('mode')=='SERVICE_PRIMARY' and a.get('scope')=='PRODUCTION' and j.get('cutover_configured') is True)
            raise SystemExit(0 if ok else 1)
          except Exception: raise SystemExit(1)
          PY
            then OK=true; break; fi
            sleep 4
          done
          [[ "$OK" == true ]] || { cat /tmp/discovery.json; exit 1; }
          curl -fsSL -H 'content-type: application/json' "$WEB_URL" -d '{"action":"health"}' > /tmp/health.json
          grep -q '"ok":true' /tmp/health.json
          grep -q '"sheet_read":true' /tmp/health.json
          curl -fsSL -H 'content-type: application/json' "$WEB_URL" -d '{"action":"update_check","channel":"BETA","current_version":"999.999.999"}' > /tmp/beta.json
          curl -fsSL -H 'content-type: application/json' "$WEB_URL" -d '{"action":"update_check","channel":"STABLE","current_version":"999.999.999"}' > /tmp/stable.json
          grep -q '"source":"GOOGLE_DRIVE"' /tmp/beta.json; grep -q '"channel":"BETA"' /tmp/beta.json; grep -q '"available":false' /tmp/beta.json
          grep -q '"source":"GOOGLE_DRIVE"' /tmp/stable.json; grep -q '"channel":"STABLE"' /tmp/stable.json; grep -q '"available":false' /tmp/stable.json
          echo 'LIVE_GAS_V140_M2_DISCOVERY_PASS'
          cat /tmp/discovery.json

      - name: Upload sanitized verification evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: gas-v140-m2-live-evidence
          path: |
            /tmp/version.json
            /tmp/discovery.json
            /tmp/health.json
            /tmp/beta.json
            /tmp/stable.json
          if-no-files-found: ignore
          retention-days: 14
