name: Beta124 apply back navigation harness
on:
  push:
    branches: [release/beta124-qr-postscan-owner-scope-20260905]
    paths: [.github/workflows/beta124-apply-back-navigation-harness.yml]
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - name: Patch harness only
        run: |
          set -euo pipefail
          python3 tools/beta124_patch_back_navigation_harness.py
          git diff --check
          grep -Fq 'invokeActivityBack' tools/Beta83UiChecksInstrumentation.java
          grep -Fq 'post_scan_activity_back_beta124' tools/beta83_verify_matrix.sh
      - name: Commit harness
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add tools/Beta83UiChecksInstrumentation.java tools/beta83_verify_matrix.sh
          git commit -m 'test(beta124): verify post-scan Activity back navigation'
          git pull --rebase origin release/beta124-qr-postscan-owner-scope-20260905
          git push origin HEAD:release/beta124-qr-postscan-owner-scope-20260905
